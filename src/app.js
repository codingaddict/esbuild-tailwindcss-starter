import classes from './app.module.css';
import Layout from 'layout';

const App = () => {
    return (
        <div className={classes.root}>
            <Layout />
        </div>
    );
};

export default App;
